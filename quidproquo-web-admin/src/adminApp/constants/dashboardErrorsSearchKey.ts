// Fixed key: the dashboard's rolling 7-day error sweep always lands in the
// same volatile cache slot instead of minting a new one per mount.
export const dashboardErrorsSearchKey = 'dashboard-errors';
