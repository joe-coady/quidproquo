// Global config key holding the name of the APPLICATION websocket api (not the
// admin's own socket) that maintenance messages broadcast on. Set via
// defineAdminSettings' `maintenanceWebsocketApiName`; empty = not configured.
export const QPQ_ADMIN_MAINTENANCE_WS_API_GLOBAL = 'qpq-admin-maintenance-ws-api';
