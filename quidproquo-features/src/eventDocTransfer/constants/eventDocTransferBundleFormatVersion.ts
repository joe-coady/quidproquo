// Bundle wire format. Bumped only on a breaking change to EventDocBundle; the import side
// refuses a bundle it does not recognise rather than guessing at a newer shape.
export const EVENT_DOC_TRANSFER_BUNDLE_FORMAT_VERSION = 1;
