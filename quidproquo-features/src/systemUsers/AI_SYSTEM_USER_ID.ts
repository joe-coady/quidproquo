// The well-known user id for actions authored by an AI assistant (server-side
// tool executions, generated events). Not a real directory user — a reserved
// system identity, shared across services so audit trails stay consistent.
export const AI_SYSTEM_USER_ID = 'system-ai';
