// The tenant document's schema version, stamped on every event by the editor.
// Must agree with `foldTenantDocument`'s `latestVersion` (the fold lives in this
// same feature).
export const TENANT_VERSION = 1;
