// The `data` payload of the chrome stream's open/close toggle events, shared by the
// api verbs (authoring) and the fold reducer (consuming).
export type EventDocWorkspaceChromeSetOpenData = { open: boolean };
