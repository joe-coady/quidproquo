import { Nullable } from 'quidproquo-core';

// The `data` payload of the chrome stream's history-panel scope event.
export type EventDocWorkspaceChromeSetHistorySlotKeyData = { slotKey: Nullable<string> };
