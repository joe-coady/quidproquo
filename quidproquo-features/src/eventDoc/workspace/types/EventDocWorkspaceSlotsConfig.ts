import { EventDocWorkspaceSlotConfig } from './EventDocWorkspaceSlotConfig';

// The `slots` map of a workspace definition; keys are fixed at definition time.
export type EventDocWorkspaceSlotsConfig = Record<string, EventDocWorkspaceSlotConfig>;
