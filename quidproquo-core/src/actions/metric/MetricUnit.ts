// Values are the CloudWatch unit strings.
export enum MetricUnit {
  count = 'Count',
  milliseconds = 'Milliseconds',
  seconds = 'Seconds',
  bytes = 'Bytes',
  percent = 'Percent',
  none = 'None',
}
