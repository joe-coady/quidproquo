/**
 * Neo4j major versions this package can target. The value doubles as the
 * actionProcessor folder name the runtime loads processors from.
 */
export enum Neo4jVersion {
  Version5 = 'version5',
}
